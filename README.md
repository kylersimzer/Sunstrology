# Sunstrology
A sun-centered birth chart in a single HTML file. Real orbital mechanics, Earth as the marker on the wheel — no retrogrades, no houses, no borrowed zodiac. Runs entirely in your browser. No installs, no accounts, no uploads.
